[project]
name = "llm-client"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv",
    "groq",
    "openai",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]